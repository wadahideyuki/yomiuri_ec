php_value date.timezone Asia/Tokyo

# ----------
# auth
# ----------
#AuthUserFile /home/giants-dev/.htpasswd
#AuthGroupFile /dev/null
#AuthName "Staff Page"
#AuthType Basic
#require valid-user


# ----------
# add type
# ----------
AddType video/ogg .ogg .ogv
AddType video/mp4 .mp4
AddType audio/ogg .ogg
AddType application/ogg .ogg .ogv

<Files ~ "\.(mov|mp4|m4v|flv|f4v)$">
Satisfy any
Allow from all
</Files>


# ----------
# deny index
# ----------
# Options -Indexes


# ----------
# use php in html
# ----------
# AddHandler application/x-httpd-php .php .html


# ----------
# RewriteEngine on
# ----------
# RewriteEngine on


# ----------
# URL normalize
# ----------
# 'index.(html|php)'' -> '/' (本番のみ適用)
# RewriteCond %{SERVER_PORT} ^443$
# RewriteRule ^(.*)index.(html|php)$ https://dev.giants-seat.jp/$1 [R=301]

# RewriteCond %{SERVER_PORT} ^80$
# RewriteRule ^(.*)index.(html|php)$ http://dev.giants-seat.jp/$1 [R=301]

# 末尾をスラッシュにする
# RewriteCond %{REQUEST_URI} !/$
# RewriteCond %{REQUEST_URI} !\.[^/\.]+$
# RewriteRule .* %{REQUEST_URI}/ [R=301,L]

# https->http
# RewriteCond %{SERVER_PORT} ^443$
# RewriteCond %{REQUEST_URI} !(^/form/)
# RewriteRule ^(.*)$ http://dev.giants-seat.jp%{REQUEST_URI} [R=301,L]

# http->https (本番のみ適用)
# RewriteCond %{SERVER_PORT} ^80$
# RewriteCond %{REQUEST_URI} /form/
# RewriteRule ^(.*)$ https://giants-seat.jp%{REQUEST_URI} [R=301,L]

# 443はwwwなし (本番のみ適用)
# RewriteCond %{SERVER_PORT} ^443$
# RewriteCond %{HTTP_HOST} ^www(.*)
# RewriteCond %{REQUEST_URI} /form/
# RewriteRule ^(.*)$ https://giants-seat.jp%{REQUEST_URI} [R=301,L]

# 80はwwwあり (本番のみ適用)
# RewriteCond %{SERVER_PORT} ^80$
# RewriteCond %{HTTP_HOST} !(^www(.*))
# RewriteCond %{REQUEST_URI} !(^/form/)
# RewriteRule ^(.*)$ http://www.giants-seat.jp%{REQUEST_URI} [R=301,L]


# ----------
# 301 redirect
# ----------
# Redirect permanent /campaign2016/ http://dev.giants-seat.jp/
# Redirect permanent /pc/buy/ http://dev.giants-seat.jp/applications/
# Redirect permanent /pc/seat.html http://dev.giants-seat.jp/seat/
# Redirect permanent /PC/ http://dev.giants-seat.jp/form/
# Redirect permanent /pc/ http://dev.giants-seat.jp/



#ErrorDocument 503 /mente.html

#<IfModule mod_rewrite.c>
#  RewriteEngine On
#  RewriteCond %{REQUEST_URI} !=/mente.html
#  RewriteCond %{REMOTE_ADDR} !=61.126.188.59
#  RewriteRule ^.*$ - [R=503,L]
#</IfModule>













